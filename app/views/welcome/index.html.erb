<br>
<br>
<br>
<br>
<br>
<br>

<%= link_to "Make a new event", new_event_path %>

      <% @events.each do |z| %>
        <%= z.id %> 
        <%= link_to z.title, event_path(@events) %>
        <%= link_to "Edit", edit_event_path %>
        <%= link_to "Destroy", event_path, method: :delete %>
      <% end %>
